#include "bits/stdc++.h"
#define rep(i,n) for (int i = 0; i < (int)(n); i++)
using namespace std;

int main()
{
  vector<int> arr = {5,1,2};
  for (int x : arr) {
    cout << x << endl;
  }
  return 0;
}
