#include "bits/stdc++.h"
#define rep(i,n) for (int i = 0; i < (int)(n); i++)
using namespace std;

int main()
{
  string s,target;
  cin >> s >> target;
  char h = target.at(0);
  cout << "A" << h << "C" << endl;
  return 0;
}
