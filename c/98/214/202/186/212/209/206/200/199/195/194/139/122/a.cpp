#include "bits/stdc++.h"
#define rep(i,n) for (int i = 0; i < (int)(n); i++)
using namespace std;

int main()
{
  char ch;
  cin>>ch;
  if (ch == 'A') {
    cout<<("T")<<endl;
  } else if (ch == 'C') {
    cout<<("G")<<endl;
  } else if (ch == 'G') {
    cout<<("C")<<endl;
  } else if (ch == 'T') {
    cout<<("A")<<endl;
  }
  return 0;
}
