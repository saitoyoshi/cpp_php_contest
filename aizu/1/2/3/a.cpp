#include "bits/stdc++.h"
#define rep(i,n) for (int i = 0; i < (int)(n); i++)
using namespace std;

int main()
{
  int a,b;
  cin >> a >> b;
  int s = a * b;
  int syu = 2 * a + 2 * b;
  cout << s << " " << syu << endl;
  return 0;
}
